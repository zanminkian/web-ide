{
  "name": "code-server",
  "version": "0.0.7",
  "private": true,
  "devDependencies": {
    "@changesets/cli": "^2.26.0"
  },
  "scripts": {
    "release": "docker login -u $USERNAME -p $PASSWORD && docker build -t zengmingjian/code-server:$(node -e \"console.log(require('./package.json').version)\") . && docker push zengmingjian/code-server:$(node -e \"console.log(require('./package.json').version)\") && echo 'New tag:'"
  }
}
